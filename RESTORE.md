# 복원 안내서

**이 문서는 AI 에이전트가 읽는 것을 전제로 씁니다.**
저장소만 클론한 상태에서 개발 환경을 다시 세우는 절차예요.

저장소에는 **코드와 문서만** 들어 있습니다. 인증키·모델 가중치·데이터셋·시안 이미지는
용량이나 규정 때문에 빠져 있어요. 아래는 그 빠진 것들을 채우는 목록입니다.

> 에이전트에게: 사용자에게 요청해야만 얻을 수 있는 항목에 **[사용자 필요]** 를
> 붙였습니다. 그 항목은 추측하거나 임의로 만들어내지 말고 사용자에게 요청하세요.

---

## 0. 먼저 읽을 문서

| 파일 | 내용 |
|---|---|
| `FEED` | **가장 최신 결정 사항.** 여기부터 읽으세요 |
| `my-way/CLAUDE.md` | 구현 규칙. §2 는 절대 규칙 |
| `AI` | 국내/국외 AI 선택 비교 |
| `CAREERNET.md` | 커리어넷 실측 정보 (실험으로 알아낸 것) |
| `TRAINING.md` | LoRA 학습 재현 정보 |
| `db-plan.md` | 인프라·개인정보 제약 |

**충돌하면 `FEED` 가 최신입니다.** 다른 문서가 `FEED` 와 어긋나면 `FEED` 를 따르고
사용자에게 알리세요.

---

## 1. 즉시 되는 것 — 인증 없이

```bash
cd my-way
npm install
npm run build          # 프론트 빌드
npx vitest run         # 테스트
```

**AI 와 커리어넷 없이도 앱은 돌아갑니다.** 두 기능은 조용히 빠지도록 폴백이
들어가 있어요. UI 확인·리팩터링은 여기까지만으로 가능합니다.

---

## 2. 커리어넷 Open API 키 [사용자 필요]

> 토스판 4단계 흐름에는 커리어넷이 없습니다. 이 절차는 **되살릴 때만** 필요해요.
> 지금 필요한지 사용자에게 먼저 확인하세요.

### 발급

1. https://www.career.go.kr → 오픈API 신청
2. 승인되면 인증키 발급. **유효기간 2년**

### 설정

```bash
cp my-way/apps/api/.env.example my-way/apps/api/.env
```

```
CAREERNET_API_KEY=발급받은키
CAREERNET_KEY_EXPIRES_AT=2028-08-24     # ISO date. 반드시 기록
```

**만료일을 꼭 채우세요.** `/api/health` 가 이 값으로 잔여일을 계산하고 60일 미만이면
경고를 냅니다. 만료되면 커리어넷 기능 전체가 멈춥니다.

### 확인

```bash
curl -s localhost:3000/api/health
# {"status":"up","key":{"configured":true,"daysLeft":721,"level":"ok"}}

curl -s -o /dev/null -w "%{http_code}\n" "localhost:3000/api/inspect/questions?q=99"
# 400  ← 화이트리스트 밖은 거부돼야 정상
```

**주의**: `report` 를 호출하면 커리어넷에 실제 응답 기록이 남습니다.
테스트로 반복 호출하지 마세요. 과거 형식 실험 때 더미 5건이 생겼습니다.
자세한 건 `CAREERNET.md` §3.

---

## 3. Google Cloud · Vertex AI [사용자 필요]

`FEED` 기준 확정 구성입니다.

```
모델    gemini-2.5-flash (128k)
리전    asia-northeast3  (서울)
```

### 준비

1. GCP 프로젝트 생성 (또는 기존 것)
2. Vertex AI API 활성화
3. 서비스 계정 + 키 발급 → `GOOGLE_APPLICATION_CREDENTIALS` 로 경로 지정

### ★ 리전 규칙 — 어기면 데이터가 국외로 나감

- **`asia-northeast3` 를 상수로 하드코딩하세요. 환경변수로 두지 마세요.**
  SDK 기본값이 `us-central1` 이라, 환경변수가 비면 조용히 미국으로 갑니다.
- **전역(global) 엔드포인트 사용 금지.** 문서에 "리전 격리 또는 데이터 레지던시
  보장 없음" 이 명시돼 있습니다.
- 코드 테스트 + GCP 조직 정책 **둘 다** 걸어두세요.

서울 리전에서 쓸 수 있는 생성 모델은 2.5 Flash 하나뿐입니다. 다른 모델로 바꾸려면
리전 지원 여부를 먼저 확인하세요.

---

## 4. 로컬 AI (선택) — 사이드 프로젝트용

> 토스판은 Vertex 를 씁니다. 이 절차는 로컬 모델을 다룰 때만 필요해요.

### 파이썬 환경

```bash
python -m venv .venv
.venv/Scripts/pip install -r requirements-restore.txt
```

### 베이스 모델 [사용자 필요]

허깅페이스에서 `LGAI-EXAONE/EXAONE-3.5-7.8B-Instruct` 를 `./exaone_model/` 로.
약 30GB 입니다. 접근 동의가 필요할 수 있어요.

### 추론 서버

```bash
.venv/Scripts/python my-way/apps/ai/server.py
# 127.0.0.1:8000 — 외부 주소로 바꾸지 마세요
```

모델 로딩에 30~60초, VRAM 약 7GB 를 씁니다.

### LoRA 어댑터

저장소에 **없습니다** (AI허브 파생물, 159MB).
외부 매체에 백업해 뒀다면 `./exaone_career_lora/` 로 복사하세요.
없으면 `TRAINING.md` 를 보고 재학습하면 됩니다.

**어댑터가 없어도 서버는 뜹니다.** 현재 두 트랙 다 `useAdapter: false` 라
베이스 모델만으로 동작해요. 오히려 그쪽이 품질이 낫습니다 (`TRAINING.md` §5).

---

## 5. AI허브 데이터셋 (선택) [사용자 필요]

> 재학습할 때만 필요합니다.

```
AI허브 → 108. 진로문장완성검사 텍스트 데이터
```

계정 로그인 + 이용 신청이 필요합니다. 승인까지 시간이 걸려요.

### ★ 압축을 전부 풀지 마세요

```
TL.zip  안:  초등학교 9,534 · 중학교 10,201 · 고등학교 12,602
풀 것:       고등학교/ 만
```

**초등학교 데이터는 영구 제외입니다.** 범위를 넓히더라도 중학교까지만 추가하세요.
자세한 건 `TRAINING.md` §1.

### 규정

- **국외 반출 금지** — 학습한 모델을 쓰면 해외 API·해외 호스팅이 막힙니다
- **제3자 제공 금지** — 데이터도 파생 모델도 저장소에 올리지 마세요
- 사용 시 **NIA 출처 표시를 앱 안에** 노출해야 합니다

---

## 6. 시안 이미지 [사용자 필요]

`.gitignore` 가 시안·스크린샷을 제외합니다. 코드 주석이 참조하는 것들이 있어요.

| 파일 | 참조하는 곳 | 없어도 되나 |
|---|---|---|
| `카드.png` | `cardCanvas.ts`, `CardRoute.tsx` 주석 | 됨. 레이아웃은 코드에 이미 반영됨 |
| `aiMK.png` | 원본. 사본이 `src/assets/aiMark.png` 에 있음 | 됨. 사본은 저장소에 있음 |

**빌드에는 지장이 없습니다.** 시안을 보고 UI 를 고쳐야 할 때만 사용자에게 요청하세요.

---

## 7. 토스 미니앱 배포 [사용자 필요]

- 앱인토스 개발자 등록 · 앱 생성
- `my-way/apps/web/apps-in-toss.config.ts` 확인
- 백엔드(`apps/api`) 를 **국내 리전**에 배포하고 `VITE_API_BASE_URL` 설정
  - Vite 프록시는 개발 서버 전용이라, 배포본에는 절대 주소가 필요합니다
- `ALLOWED_ORIGINS` 를 배포 도메인으로 채우세요
  - **비워두면 모든 출처를 허용합니다.** 커리어넷 키가 노출될 수 있어요

### 선행 확인

`FEED` 에 **WebView Origin 변경** 항목이 있습니다. SDK 3.1.1 이상으로 빌드하면
Origin 이 바뀌어 **localStorage 가 통째로 비워집니다.** 저장 키가 22개예요
(`my-way:` 접두사). 저장 기능을 붙이기 전에 마이그레이션을 먼저 처리하세요.

---

## 8. 코드 밖에서 닫아야 하는 것

`FEED` 의 "미해결" 항목입니다. **코드로 못 푸는 것들이라 여기 옮겨 둡니다.**

- [ ] **만 14세 미만 법정대리인 동의** — 중1은 만 12~13세. 파일럿 포함 시 필수.
      절차라 시간이 걸리므로 가장 급합니다
- [ ] 서비스 약관 Data Location 조항 발췌 (법무 검토 근거)
- [ ] 개인정보처리방침 위탁 공개 — 수탁자 2곳 (한국직업능력연구원, Google)
- [ ] 국외이전 해당 여부 법무 판단
      (참고: 개인정보보호위원회 「생성형 AI 개발·활용을 위한 개인정보 처리 안내서」 2025.8)
- [ ] 필터 통과율 실측 — 모델을 바꾸면 다시 재야 합니다

---

## 9. 복원 확인 체크리스트

```bash
cd my-way
npm install && npm run build          # 빌드 통과
npx vitest run                         # 테스트 통과
npx tsc -b apps/web apps/api           # 타입 통과

# 번들에 키가 섞이지 않았는지
cd apps/web && grep -ri "CAREERNET_API_KEY\|apikey=" dist/ && echo "키 노출!" || echo "OK"

# 결과 URL 하드코딩 없는지
grep -ri "inspct/web\|inspect/ready" dist/ && echo "URL 하드코딩!" || echo "OK"
```

전부 통과하면 코드 쪽 복원은 끝입니다. 나머지는 위의 [사용자 필요] 항목입니다.
