# 학습 재현 정보 — 사이드 프로젝트용

작성 2026-09-05.

**이 문서의 목적은 하나입니다.** `exaone_career_lora/` 와 데이터셋은 규정·용량 때문에
git 에 올릴 수 없습니다. 그래서 **가중치 대신 만드는 법을 글로 남깁니다.**
나중에 사이드 프로젝트를 시작할 때 이 문서 하나로 같은 자리에서 출발할 수 있어야 해요.

---

## 0. 백업되지 않는 것 · 어디 있나

| 대상 | 크기 | 왜 안 올라가나 | 지금 위치 |
|---|---|---|---|
| `exaone_career_lora/` | 159MB | AI허브 파생물. 제3자 제공·국외 반출 금지 | 프로젝트 폴더 |
| AI허브 108번 데이터셋 | 61MB | 위와 같음. **애초에 프로젝트 폴더 밖** | `~/Downloads/108.진로문장완성검사 텍스트 데이터/` |
| `exaone_model/` | 30GB | 파일 상한 초과 | 허깅페이스에서 재취득 |

**셋 다 다시 만들 수 있습니다.** 데이터셋은 AI허브에서 다시 받고, 베이스 모델은
허깅페이스에서 받고, 어댑터는 아래 설정으로 다시 학습하면 됩니다.
그래서 진짜 잃으면 안 되는 건 파일이 아니라 **아래 정보**예요.

---

## 1. 데이터셋

```
AI허브 108. 진로문장완성검사 텍스트 데이터
  └ 3.개방데이터 / 1.데이터 / Training
```

`AI.py` 의 `TRAIN_DIR` 이 이 경로를 가리킵니다. 로더는 하위를 `rglob("*.json")` 로 훑고,
경로에 `02.라벨링데이터` · `TL` · `라벨링` 이 든 것만 골라 씁니다.

> 이 필터 덕분에 원천데이터(TS)는 처음부터 읽히지 않았어요. 그래서 지워도
> 학습 결과가 달라지지 않습니다.

### 파싱 대상

두 가지 형태를 받습니다.

- `comment` 객체 → `question` 을 user, `answer_1`(없으면 `answer_full`)을 assistant
- `test` 배열 → `meta` 의 `question_meta` 로 문항을 찾아 매칭

즉 학습 타깃이 **`제시어 → 학생 답변`** 입니다. 이게 나중에 문제가 됩니다(§5 참고).

### 학교급 — 필터는 코드가 아니라 **압축 해제 범위**였다

`AI.py` 에는 학교급을 거르는 코드가 **없습니다.** 그런데 실제 학습은 고등학교로만
돌아갔어요. 이유는 `TL.zip` 에서 **고등학교만 풀었기 때문**입니다.

원본 `TL.zip` 의 구성은 이랬습니다:

| 학교급 | 라벨링 json |
|---|---|
| 초등학교 | 9,534 |
| 중학교 | 10,201 |
| **고등학교** | **12,602** ← 학습에 쓴 것 |

**숫자가 맞습니다.** 12,602건 ÷ 배치 4 ≈ 3,150 → 실제 `global_step` 3,149 와 일치해요.
학습이 정확히 고등학교 전량으로 돌았다는 뜻입니다.

### 지금 디스크에 남아 있는 것 (2026-09-06 정리)

**zip 과 원천데이터(TS·VS)를 지웠습니다.** 라벨링 데이터만 풀린 채로 남아 있어요.

```
108.진로문장완성검사 텍스트 데이터/3.개방데이터/1.데이터/
├ Training/02.라벨링데이터/TL/고등학교/     12,602 json   학습용
└ Validation/02.라벨링데이터/VL/고등학교/    1,583 json   평가용
```

- **원천데이터(TS·VS)는 상담사 코멘트가 없어서** 지웠습니다. `meta` + `test` 구조라
  §6 의 개선 방향에 쓸 수 없어요. 필요하면 AI허브에서 다시 받으세요.
- **디스크에 초등학교 데이터가 아예 없습니다.** 영구 제외 원칙이 파일 수준에서
  지켜진 상태예요.

### 다시 할 때

지금 있는 것으로 재학습하려면 `TRAIN_DIR` 을 위 `TL` 경로로 두면 그대로 돕니다.

**중학교까지 넓히려면 AI허브에서 다시 받아야 합니다.** zip 을 지웠기 때문에
로컬에서 꺼낼 방법이 없어요. 그때는 `load_aihub_dataset` 에 학교급 필터를 넣으세요 —
압축을 잘못 풀어도 코드가 막아줍니다. **초등학교는 영구 제외입니다.**

> 폴더 이름에 **학교 실명**이 들어갑니다(`가락고등학교` 등). 학습에는 안 쓰이지만,
> 경로를 로그나 에러 메시지에 찍지 마세요. (`CLAUDE.md §2.8`)

---

## 2. 환경

```
python        3.12.10
torch         2.11.0+cu128
transformers  5.16.1
peft          0.20.0
trl           1.12.0
GPU           RTX 5060 Ti (8GB)
```

`requirements-restore.txt` 로 복원됩니다. **버전을 올릴 때 §3 패치가 필요 없어졌는지
먼저 확인하세요.** 상류에서 고쳐졌으면 패치를 지우는 편이 낫습니다.

---

## 3. 호환성 패치 두 개 — 없으면 학습이 시작조차 안 됨

transformers 5.x 와 EXAONE 구현이 어긋나는 지점입니다. 둘 다 `AI.py` 안에 있지만
**왜 필요한지가 코드만 봐서는 안 보여서** 여기 남깁니다.

### (1) `create_causal_mask` 의 인자 이름

EXAONE 쪽이 `input_embeds` 로 넘기는데 transformers 는 `inputs_embeds` 를 받습니다.
게다가 상류 시그니처에 없는 인자까지 딸려 와서, 이름만 고쳐선 안 되고 **받지 않는
인자를 걸러내야** 합니다.

```
TypeError: create_causal_mask() got an unexpected keyword argument 'input_embeds'
```

`transformers.masking_utils` 를 감싸서 처리합니다. 예전에 다른 모듈을 패치해 둔 게
있었는데 **조용히 아무 일도 안 하고 있었습니다.** 대상 모듈을 확인하세요.

### (2) `_input_embed_layer` 누락

`prepare_model_for_kbit_training` 이 이 속성을 찾는데 EXAONE 에 없습니다.
모델 로드 뒤, 준비 함수 호출 **전에** `"wte"` 로 지정해야 합니다.

---

## 4. 학습 설정

### 4bit 양자화

```python
load_in_4bit=True
bnb_4bit_quant_type="nf4"
bnb_4bit_compute_dtype=bfloat16
bnb_4bit_use_double_quant=True
```

### LoRA

```python
r=16, lora_alpha=32, lora_dropout=0.05, bias="none"
target_modules=["q_proj","k_proj","v_proj","o_proj","gate_proj","up_proj","down_proj"]
```

학습 파라미터 9,437,184개 (베이스의 0.121%), 산출물 18.9MB.

### SFTConfig

```python
per_device_train_batch_size=4
gradient_accumulation_steps=1
learning_rate=2e-4
num_train_epochs=1
max_length=128
gradient_checkpointing=True, use_reentrant=False
optim="paged_adamw_8bit"
save_strategy="steps", save_steps=500, save_total_limit=3
```

`max_length=128` 과 `epochs=1` 은 **시간을 줄이려고 낮춘 값**입니다. 품질을 노린
설정이 아니에요. 8GB VRAM 이라 배치를 더 못 키웁니다.

체크포인트에서 자동 재개하도록 `get_last_checkpoint(OUTPUT_DIR)` 를 씁니다.
중간에 끊겨도 이어서 돌아가요.

---

## 5. 결과 — 그리고 왜 껐는가

### 학습 지표

```
global_step  3149  (1 epoch 완주)
loss         0.31
token 정확도  0.92
학습 토큰     784,928
```

샘플 수는 3,149 × 배치 4 ≈ 12,600건입니다.

### 지표는 좋은데 앱에는 못 씁니다

`제시어 → 학생 답변` 으로 학습해서 **상담사가 아니라 학생을 흉내냅니다.**
되물을 질문을 만들어야 하는 자리에 학생 말투의 서술이 나와요.

같은 프롬프트로 잰 히트율 (`/api/ai/followup` 이 쓸 만한 질문을 돌려준 비율):

| 트랙 | 어댑터 켬 | 어댑터 끔(베이스) |
|---|---|---|
| teen | 1/10 | 5/5 |
| jobseeker | 1/10 | 10/10 |

```
어댑터 ON   "아직까지는 없다"                              (1.4초)
어댑터 OFF  "그럼 어떤 활동이나 취미를 할 때 자신감이
             생기거나 흥미를 느끼는 부분은 있어?"           (3~5초)
```

어투도 베이스가 낫습니다. 프롬프트로 반말·해요체를 지시하면 그대로 따라오는데,
어댑터를 켜면 지시와 무관하게 고등학생 반말로 새어나옵니다.

**그래서 두 트랙 다 `useAdapter: false` 입니다.** 스위치는 `apps/ai/server.py` 에
남아 있어요(PEFT `disable_adapter()` 컨텍스트). 요청마다 켜고 끌 수 있습니다.

---

## 6. 다음에 할 것 — 학습 타깃 바꾸기

원본 데이터에 **아직 안 쓴 필드가 셋** 있습니다.

| 필드 | 내용 |
|---|---|
| `comment.comment` | 상담사 코멘트. 100% 존재, 평균 56자 |
| `meta.category_name` | 영역 8종 |
| `comment.grade` | 등급 상/중/하 |

타깃을 **`(제시어 + 학생답변) → 상담사 코멘트`** 로 바꾸면 앱에 실제로 쓸 수 있는
모델이 나옵니다. 지금은 셋 다 안 쓰고 있어요.

> ⚠ 단, 상담사 코멘트는 평가적 문장이라 **그대로 화면에 띄우면 `CLAUDE.md §7` 위반**
> 입니다. 학습에는 써도 출력은 §7 필터를 반드시 통과시키세요.
> 필터는 `apps/api/src/routes/ai.ts` 에 있고 테스트가 붙어 있습니다.

---

## 7. 잊지 말 제약

- **초등학교 데이터는 영구 제외.** 넓히더라도 중학교까지.
- **AI허브 파생물은 국외로 나갈 수 없습니다.** 학습한 모델을 쓰는 순간 해외 API·해외
  호스팅이 막힙니다. 베이스 모델만 쓰는 지금과 조건이 달라져요. (`AI` 문서 참고)
- **`cues` 같은 라벨 기준을 프롬프트에 심지 마세요.** 프롬프팅이라도 국외 반출에
  해당합니다.
- 추론 서버는 `127.0.0.1` 바인딩을 유지하세요.
