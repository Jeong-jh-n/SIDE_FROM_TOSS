# 커리어넷 Open API 실측 정보

작성 2026-09-05.

**이 문서의 목적.** 토스판은 커리어넷을 쓰지 않는 4단계 흐름으로 바뀌었고,
영역 되묻기도 삭제 결정이 났습니다. 그 과정에서 `CLAUDE.md` 의 §6 등이 편집되고
`inspectMeta.ts` · `answers.ts` 가 쓰이지 않게 됩니다.

여기 적힌 것들은 **문서에 없어서 실험으로 알아낸 것**이라, 코드가 사라지면 다시
알아내는 데 또 시간이 듭니다. 나중에 되살릴 때를 위해 글로 남깁니다.

---

## 0. 한 줄 요약

**매뉴얼과 실제 응답이 다릅니다.** 매뉴얼 §1.3 표에 있는 `relm`(영역)이 실제로는
오지 않고, 이 사실 하나가 결과 화면 설계 전체를 바꿨습니다.

---

## 1. 응답 스펙 — 실측

| 확인 항목 | 결과 |
|---|---|
| `relm`(영역) 필드 | **응답에 없음.** 매뉴얼 표에는 있으나 안 옴 |
| `questions` 응답 | `question`, `answer01~10`, `answerScore01~10`, `tip1~3Score`, `tip1~3Desc`, `qitemNo` (28개) |
| `report` 응답 | `{ inspctSeq, url }` **뿐.** 점수 데이터 없음 |
| 결과 URL 의 `seq` | `base64(inspctSeq)` |

### `relm` 에 대한 커리어넷 답변

2020년 이후 쓰지 않는 필드이고, 개선 중이지만 **일정은 없다**고 했습니다.

**이게 설계의 출발점입니다.** 문항이 어느 하위영역인지 알 수 없으므로 **영역별 점수를
앱에서 계산할 수 없습니다.** 그래서 점수 판정은 전부 커리어넷에 맡기고, 우리는 서술
정리만 했습니다. 복구되면 이 전제가 통째로 바뀝니다.

---

## 2. 검사 번호와 대상 코드

```
GET  https://www.career.go.kr/inspct/openapi/test/questions?apikey={KEY}&q={번호}
POST https://www.career.go.kr/inspct/openapi/test/report
```

| 번호 | 검사 | 대상코드 |
|---|---|---|
| 6 | 직업가치관 – 대학생·일반 | 100208 / 100209 |
| 8 | 진로개발준비도 – 대학생 (35문항, 6영역) | 100208 / 100209 |
| 24 / 25 | 직업가치관 – 중 / 고 | 100206 / 100207 |
| 35 / 36 | 진로성숙도 – 중 / 고 | 100206 / 100207 |

`gender`: `100323` 남 · `100324` 여

화이트리스트는 `[6, 8, 24, 25, 35, 36]`. **`q` 를 그대로 통과시키면 우리 키로 아무
검사나 긁어갑니다.** (`apps/api/src/lib/careernet.ts`)

---

## 3. ★ answers 형식 — 검사마다 다르다

**매뉴얼에 없고, HTTP 500 을 격리 실험해서 알아낸 것입니다.**

| 형식 | 예 | 쓰는 검사 |
|---|---|---|
| `pairs` | `"1=5 2=7 3=4"` | 6, 24, 25, 35, 36 |
| `values` | `"4,4,5,4"` | **8** |

q=8 에 `pairs` 를 보내면 이게 돌아옵니다.

```
HTTP 500  "시스템 오류가 발생했습니다"
```

2026-08-28 에 이걸 **커리어넷 장애로 오판**했습니다. 원인은 우리 쪽 형식이었고,
`1=4 2=4` → 500 / `4,4,...` → 200 으로 갈라서 확인했습니다. 2026-08-31 에 고쳐
실제 호출로 검증했습니다 (`inspctSeq: 89964880`).

> 이 실험 과정에서 **커리어넷에 더미 응답 5건이 생성됐습니다.**
> 다시 실험할 일이 있으면 최소 횟수로 하세요.

### 왜 q=8 만 다른가

`values` 는 값만 늘어놓기 때문에 **값 안에 쉼표가 들어가는 특수 문항을 표현할 수
없습니다** (`49=8,1,4` 같은 것). 특수 문항이 있는 검사는 구조적으로 `pairs` 여야 해요.
q=8 은 특수 문항이 없어서 `values` 가 가능하고, 실제로 `values` 만 받습니다.

### 문항 번호는 배열 순서

**`qitemNo` 를 답변 번호로 쓰지 마세요.** 영역별 시퀀스라 답변 순번과 다릅니다.
배열 순서 기준으로 1부터 매기세요. 선택값은 `answerScoreNN` 을 그대로 씁니다.

---

## 4. 특수 문항

| 검사 | 처리 |
|---|---|
| 24, 25 | 49번은 3개 선택을 쉼표로 — `49=8,1,4` |
| 35 | 13번은 순위 2개. 답변 **8**(기타)은 주관식 — `13=1,8:방송계열` |
| 36 | 13번은 순위 2개. 답변 **9**(기타)는 주관식 — `13=1,9:예체능` |

**주관식 답변에 띄어쓰기가 들어가면 안 됩니다.** 입력값에서 공백을 제거하세요.

검증 상태: q=6·36 은 `pairs` 로 성공 확인. q=8 은 `values` 로 성공 확인.
q=24·25·35 는 형식만 정리했고 **실호출 미검증**입니다.

---

## 5. 진로개발준비도(q=8) 6영역 — 결과표 실측

```
자기이해 · 전공 직업지식 · 진로결정확신도
의사결정자신감 · 관계활용자신감 · 구직준비도
```

**실제 결과표에서 확인한 값**이라 상수로 두어도 됩니다.
다른 검사의 영역 목록은 확인하지 못했습니다 — 그 검사 결과표를 열어봐야 알 수 있어요.

---

## 6. 결과 URL

**URL 전체를 저장하세요.** `inspctSeq` 로 재구성하면 안 됩니다. 경로가 검사마다 달라요.

```
진로개발준비도  /cloud/w/inspect/ready/report?seq=...
매뉴얼 예시     /inspct/web/psycho/vocation/report?seq=...
```

`seq` 가 `base64(inspctSeq)` 라 **숫자만 바꾸면 타인의 결과가 열립니다.**
DOM·콘솔·로그·분석도구 어디에도 평문으로 남기지 마세요.

---

## 7. 운영 제약

- **인증키 유효기간 2년.** 만료되면 서비스 전체가 멈춥니다.
  `/api/health` 가 `CAREERNET_KEY_EXPIRES_AT` 기준 잔여일을 반환하고 60일 미만이면
  경고 레벨을 냅니다.
- **문항은 24시간 이상 캐시.** 매뉴얼에 "이용량에 따라 사용이 제한될 수 있습니다"
  라고 적혀 있습니다.
- `SUCC_YN` 이 `N` 이면 `ERROR_REASON` 은 **서버 로그에만**, 클라이언트에는 일반화된
  메시지를 주세요.
- **결과 페이지를 스크래핑하지 마세요.** 약관 위반이고 마크업 변경에 취약합니다.
- **상업적 이용 — 가능 확인 완료** (공공누리 유형 확인, 2026-09-05).

---

## 8. 영역 되묻기가 삭제된 이유

점수가 안 오니 **사용자에게 직접 묻는** 장치였습니다. 결과표를 웹뷰로 보여주고,
돌아오면 "여섯 개 중 제일 낮게 나온 게 뭐야?" 를 6지선다로 묻는 구조였어요.

**앱인토스 정책이 "핵심 기능이 외부 URL 에서 완결되는 구조"를 금지합니다.**
되묻기가 커리어넷 결과표 방문에 의존하므로 정책 위반입니다.

되살리려면 둘 중 하나가 필요합니다.

1. `relm` 복구 → 앱 안에서 영역 점수를 계산 (커리어넷 일정 없음)
2. 결과를 앱 안에서 완결시킬 다른 방법

`READY_AREAS` 상수와 §5 의 영역 목록은 1번이 열릴 때 바로 쓸 수 있습니다.

---

## 9. 관련 파일 (지우기 전 위치)

```
apps/api/src/lib/careernet.ts       화이트리스트 · 호출
apps/api/src/lib/cache.ts           24시간 문항 캐시
apps/api/src/routes/inspect.ts      프록시 라우트
apps/web/src/lib/answers.ts         ★ 형식 분기 · 특수 문항 조립
apps/web/src/lib/inspectMeta.ts     검사 메타 · READY_AREAS
apps/web/src/features/inspect/      검사 실시 UI · AreaAskback
```

**지우지 말고 남겨두는 편을 권합니다.** 토스판 빌드에는 안 들어가고,
되살릴 때 `answers.ts` 하나만으로도 실험 며칠을 아낍니다.
